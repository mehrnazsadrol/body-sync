import 'package:flutter/material.dart';

class WorkoutView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Workout View', style: TextStyle(fontSize: 24)),
    );
  }
}
